The file named BST implements the methods of a binary search tree which are: insert, delete, find, in-order traversal, pre-order traversal,
post-order traversal and print

The file named BSTtest is used to test the methods implemented in BST class by reading the commands from a text file.